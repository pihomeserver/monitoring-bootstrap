modules:
  http_2xx:
    prober: http
    timeout: 15s
    http:
      valid_status_codes: [200, 401]
      preferred_ip_protocol: "ip4"
      ip_protocol_fallback: false
      method: GET
      fail_if_not_ssl: true
